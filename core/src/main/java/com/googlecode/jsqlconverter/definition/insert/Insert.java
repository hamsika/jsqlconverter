package com.googlecode.jsqlconverter.definition.insert;

import com.googlecode.jsqlconverter.definition.Statement;

public abstract class Insert extends Statement {
	/*
	features from other RDBMS:
		PRIORITYS
		can ignore duplicate keys
		can use default
		can update on duplicate key
		can insert from select
		can limit (exact num / percent)
	 */
}
